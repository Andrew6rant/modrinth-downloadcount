# modrinth-downloadcount
Python script to print the total download count over all mods of a developer on Modrinth

Just pass a user id in get_downloads(). For example, ```get_downloads('IV9h2DkJ')```
